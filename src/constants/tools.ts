/** How long the cursor must stay within a hitbox before snapping to residual */
export const RESIDUAL_DWELL_MS = 150;

/** Hitbox slop for the residual snap */
export const RESIDUAL_MAX_DRIFT_PX2 = 20;
